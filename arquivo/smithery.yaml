# 🧜‍♀️ IaraMCP - Configuração Smithery.ai
name: iaramcp
version: "0.1.0"
description: "Servidor MCP de Análise Musical avançada inspirado na Iara, ser mitológico brasileiro das águas"

# Metadados do projeto
author: "IaraMCP"
homepage: "https://github.com/user/iaramcp"
repository: "https://github.com/user/iaramcp"
license: "MIT"

# Tags para descoberta no marketplace
tags: 
  - "audio"
  - "music"
  - "analysis" 
  - "mcp"
  - "brazil"
  - "ai"

# Configuração de build Docker
build:
  type: docker
  dockerfile: Dockerfile

# Configuração de execução
run:
  # Servidor HTTP híbrido (MCP + API)
  http:
    ports:
      - port: 3333
        # Health check no endpoint principal
        health_check:
          http:
            path: "/"
            timeout: 30s
            interval: 60s

# Variáveis de ambiente para modo remoto
environment:
  IARAMCP_VIS_MODE: "web"
  PYTHONPATH: "/app"

# Recursos necessários
resources:
  memory: "1Gi"
  cpu: "0.5"

# Configuração MCP
mcp:
  server:
    name: "IaraMCP"
    version: "0.1.0"
    description: "Análise Musical das Águas - 22 ferramentas temáticas da Iara"
    
# Documentação e links úteis  
documentation:
  readme: "README.md"
  manual: "MANUAL.md"
  technical: "AGENTE.md"